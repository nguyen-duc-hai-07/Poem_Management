<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

  <changeSet id="009-create-feedbacks" author="hazi">
    <createTable tableName="feedbacks">
      <column name="id" type="BIGSERIAL">
        <constraints primaryKey="true" nullable="false"/>
      </column>

      <column name="poem_id" type="BIGINT">
        <constraints nullable="false"
                     foreignKeyName="fk_feedbacks_poem"
                     references="poems(id)"/>
      </column>

      <column name="user_id" type="BIGINT">
        <constraints foreignKeyName="fk_feedbacks_user"
                     references="users(id)"/>
      </column>

      <column name="content" type="TEXT">
        <constraints nullable="false"/>
      </column>

      <column name="status" type="feedback_status_enum"/>

      <column name="created_at" type="TIMESTAMP"/>

      <column name="is_deleted" type="BOOLEAN"/>
    </createTable>
  </changeSet>

</databaseChangeLog>
